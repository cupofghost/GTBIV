# STATUS

Last consolidation: 2026-08-11 (thirteenth pass — archived the four open rows: the PV production-value pass, the PV follow-up, the full-game script pass, and this session's owner playtest batch BUG1–BUG6. Folded the two 2026-08-10 FIXED entries into the archive record they belong to, retired the twelve stale per-batch `HANDOFF.md` bookkeeping lines from Shared-file touches (that work is merged; the detail is in git history and the Archive), and split the "do not fix this" notes out of Known issues into their own section — the SLO-MO/joystick overlap moved there as **intended behaviour** on the owner's ruling. Suite baseline resynced to the real 288 cases across 57 files. PII/secret scan clean. Focused suites green: playtest-bugs 10/10, charged-melee 5/5, camera-polish 7/7, fall-damage 6/6, carnage 7/7, bail-out 7/7, bike-rider 5/5, shadow-footsteps 5/5, sprint 3/3, traffic-pooling 4/4, cinema-mode 6/6, respawn-flow 2/2, control-feel 2/2, rpg-flight 2/2, smoke 2/2 — full suite left to CI per AGENTS.md §4a) — Signed: Claude Code | Opus 5 | high
Previous: 2026-08-02 (twelfth pass — archived the FB3/FB4/tooling/concurrency batch; Football Saga FB1–FB5 closed in `HANDOFF.md`; code map and testing tiers now tool-backed and CI-gated) — Signed: Claude Code | Opus 5 | high

## Active work
_Sprints are ad-hoc. Owner has their own job. Expect gaps between sprints (days or weeks). This is intentional._

_Nothing in flight as of the 2026-08-11 consolidation._

| Date | Area / files | Task & state (≤3 lines) | Signature |
|------|--------------|-------------------------|-----------|

## Shared-file touches
Standing list of what's hot. If you must edit one of these, make the smallest
possible change and add a line here.
- `LESSONS.md` — weekly knowledge base. Agents add lessons learned after solving hard problems or hitting walls; keeps the team from learning the same thing twice. Signed: Claude Code \| Haiku 4.5 \| high
- `STATUS.md` — added Haiku row to Active work, shared-file touches for rewards system. Signed: Claude Code \| Haiku 4.5 \| high
- `HAIKU.md`, `CONDUCTOR.md` — coordination layer (validation, consolidation triggers) + 5-hour sprint orchestration with reward tiers & autonomy. Signed: Claude Code \| Haiku 4.5 \| high
- `tools/haiku-*.js` — four new scripts: pre-commit validation (check.js), post-commit scanning (overseer.js), work assignment (dispatch.js), performance tracking (rewards.js). Signed: Claude Code \| Haiku 4.5 \| high
- `index.html` — the whole game. Hot spots, in order of how often they conflict: the AUDIO section (the `let AC=null,…` declaration line and the `exitCarSoft()` reset block each grow a variable every time someone adds a sound layer), `updateFoot`, and the terrain/ground block (`groundH`, `terrainLines`/`terrainGeo`) — terrain is settled as of #31, so read `TERRAIN.md` before reopening it.
- `index.html`'s `// CODE MAP` comment block — its line ranges go stale the moment anyone inserts code. It is mechanical to regenerate from the section banners; don't hand-edit the numbers.
- `HANDOFF.md` — §5 code map and §8 backlog get edited by most sessions; edit the row, not the structure.
- `TERRAIN.md` — the terrain contract. Read the Tier 1 revision note before touching `groundH`.
- `js/person.js` — shared rig; additions must stay backward-compatible (limbs are read by several animation paths). **`player.mesh` alone carries `rotation.order='YXZ'`** (set where the rig is built, restored by `finishChargedMelee`) so a grade leans Turbo into the hill instead of tipping him sideways; every other `makePerson()` rig is still on the default `'XYZ'`, which is what their knockdown pose (`rotation.x=-PI/2`) is authored against. Don't change one without the other.
- `index.html` car/ped lifecycle — R2 added bounded generic free-lists and active-array retirement; keep mission/cinema exclusions intact. Signed: Codex | GPT-5 | high
- `HANDOFF.md` — the twelve per-batch bookkeeping rows Codex and Claude logged here between 2026-07-25 and 2026-07-28 (backlog inserts for OD1–OD4, OP1, AF1–AF4, RV2/OP2/TM1–TM3, the OP2 dispatch packet) covered work that is merged and archived below; retired at the 2026-08-11 consolidation. The structural rule survives them: edit the §5 row and the §8 backlog card, not the document's structure.
- `.github/workflows/ci.yml` — added `node ../tools/codemap.js` between the syntax check and the suite, so a merge that scrambles the generated CODE MAP fails the PR instead of landing as a silently-wrong comment block. Signed: Claude Code \| Opus 5 \| high
- `index.html`, `HANDOFF.md` — FB4 added §TURBO BOWL (FB4) whole, between §COACH REMATCH and §STRAY DOGS, and reached outside it in three places only: one `turbo_bowl_payoff` entry in `CUTSCENES`, one `updateTurboBowl(simDt)` beside `updateCoachMission` in the main loop, and three fields in the save blob (`turboBowlBest`/`turboBowlWon`/`turboBowlRuns`, same shape as `coachBeaten`). Defenders and the sideline cast are in their own arrays, never `jocks`/`peds`, so no faction AI can reach them; `updateFoot` and the weapon/melee paths are untouched. Signed: Claude Code \| Opus 5 \| high
- `index.html`, `HANDOFF.md` — FB3 added §COACH REMATCH (FB3) whole, between §FOOTBALL RIVALS and §STRAY DOGS, and reached outside it in six deliberately one-line ways: two predicates in `updateJocks` (`jocksHostile()` on the aggro line, `jockTauntPack(j)` on the taunt line); three `if(!j.alumni)` heat guards in `downJock`/`doPunch`/`chargedMeleeHit` so the strand never touches the cops; and one `coachMeleeHit(...)`/`coachChargedHit(s)` hook per melee path so Coach — who is in neither `jocks` nor `peds` — is reachable only while his own round is live. Plus two new `CUTSCENES` entries and one `updateCoachMission(simDt)` beside `updateStory` in the main loop. No new module-level audio nodes (the whistle is a plain function over `blip`/`noiseBurst`), and `groundH`/terrain untouched. `HANDOFF.md`: §5 row, FB3 card marked DONE, FB4 marked blocked on sign-off, NEXT handed back to the owner. Signed: Claude Code \| Opus 5 \| high
- `index.html` — PV batch (2026-08-07). New sections added whole: §POST FX (after §THREE SETUP), §SLOW MOTION and §ACTION BAIL-OUT/§RAMPAGE (before §MAIN LOOP / §HUD), §CARNAGE (end of §PARTICLES). Reached outside them in deliberately small ways: `renderFrame()` replaces the main loop's three `renderer.render()` calls; one `fxSetTier(tier)` in `applyQuality`; one-line `fxFlash`/`fxImpact`/`fxDamage`/`rampageHit` in `explode`/`bigExplosion`/`damagePlayer`/`killCar`; `updateSlowmo(dt)` at the top of `loop()` and `simDt = dt*TIME_SCALE*SLOWMO.scale`; `resetSlowmo`/`endBailDive`/`endRampage` in `busted`/`wasted`. **§AUDIO hot spot:** one new node, `slowLP`, a master-bus lowpass created lazily inside §SLOW MOTION — it is NOT on the `let AC=null,…` line and needs no `exitCarSoft()` reset (it self-restores to 22kHz). **`groundH` (terrain contract):** one added term gated on `m > SHORE_START` (H+14) — beach only, no road/block/building reaches it, mesh-vs-field error measured 0.0000u. `js/person.js` untouched. Signed: Claude Code \| Opus 5 \| high
- `index.html` — PV follow-up (2026-08-10), two small edits inside the PV batch's own blast radius. `updateFoot`: the grounding block now absorbs a travelled-scaled step-down before the fall path, so a downhill jog stops re-landing (and re-syncing the gait) every frame — jumps, ledges and real drops are untouched, the allowance caps at 0.6u. Fire-escape/straight-stair generation: a run's `baseH` is now `max(b.baseY, groundH(stair foot))` instead of the building's base, so a flight standing on graded ground starts on that ground rather than inside it. `groundH` itself, the terrain field and `js/person.js` are untouched. Signed: Claude Code \| Opus 5 \| high
- `index.html`, `js/person.js` — OP2 five-agent batch, now integrated: `updatePersonShadows(simDt)` in the main loop; footsteps as plain functions after the `sfx` registry (no new module-level audio nodes, own LCG, so the `let AC=null,…` line, `exitCarSoft()`'s reset block and `_rng()` determinism are all untouched); `g.userData.shadow` exposed additively on the person rig; one `endMelee()` beside each existing `clearSprint()`/`disarmSprint()`; the cinematic route planner beside `flySample`, with `cinematicBuildingVolumes()` cached. Signed: Claude Code | Opus 5 | high
- `index.html` — owner playtest batch (2026-08-11), edits in five sections. **§CAMERA:** `cameraCollide` now bisects between its last clear probe sample and its first blocked one (same answer, continuous instead of tenths-of-a-boom), and both the car and foot branches route their occlusion follow through one new `camFollow(tgt,outRate,dt)` — pull-in still beats push-out, it is just speed-capped at 22 u/s instead of being an instant teleport. Nothing else in `updateCamera` moved; `boomCam` and the interior camera are untouched. **§CARS / §HELICOPTERS / the player rig:** one line each — `rotation.order='YXZ'` in `makeCarMesh`, in `makeHeliMesh`, and on `player.mesh` where it is built (plus the matching restore in `finishChargedMelee`, which used to reset to `'XYZ'`). No attitude *values* changed anywhere; the composition order did. **melee:** `canMelee()` split into `meleeAllowed()` (no strike-recovery timers) + the timer gate, `canChargeMelee()` split into `canMelee()+chargeStanceOk()`, and `meleePress()` now fires `doPunch` itself and supersedes a stale hold; `meleeRelease()` only clears the hold. **§CARNAGE:** new gore block added whole between the scorch pool and the chain reactions (`COL_BLOOD`, `GIB_COLS`, the `bloodPools` pool, `addBloodPool`, `updateBlood`, `gibTurbo`) — it reuses the existing bounded `debris` pool for chunks, adds no audio node, and reaches outside itself twice: one `updateBlood(simDt)` beside `updateScorch` in the main loop, and `wasted(); gibTurbo(...)` replacing the smoke puff in `applyFallImpact`. `groundH`/terrain and `js/person.js` untouched. Signed: Claude Code \| Opus 5 \| high

## Design decisions — do not "fix" these
Things that look like bugs, were checked, and are staying. Each one has already
cost a session; read before "correcting" any of them.
- **The SLO-MO button doubling as a joystick grab is INTENDED — owner's ruling, 2026-08-11.** PV9's `#btnSlow` is a plain `<div id="btnSlow">`, not a `.tbtn`, and the document-level `touchstart` joystick handler only skips `.tbtn` (`e.target.closest('.tbtn')`) — so pressing SLO-MO inside the joystick's screen region (left 45%, below 35%) starts a joystick drag from that point too. Reported to the owner as a defect; they play one-thumbed and want it: *"I actually like how the slow Mo button grabs the joystick and functions since I don't have two left thumbs."* Do not add the class, do not widen the selector, and do not move the button out of the joystick region. — 2026-08-11, Claude Code
- **`FOOTBALL_STRAND.md` §6 and `HANDOFF.md`'s FB5 card describe incompatible Turbo Bowl payoffs; the FB5 card is canonical.** The strand doc has Reverend Jones storm the field with three shouted lines; the FB5 card says "Dad never appears on-screen … don't add a Dad actor/model for this" and has Turbo wave the squad off himself. **The owner's ruling (2026-08-02) was the FB5 card**, and that is what shipped — `turbo_bowl_payoff` has no `DAD` speaker and `tests/cases/turbo-bowl.test.js` asserts it stays that way. The story doc was left as the owner wrote it rather than rewritten. Do not "fix" the code to match the strand doc. — 2026-08-02, Claude Code
- **Do not repin `vehicle-sanity.test.js`'s seed to one that passes.** See the open entry under Known issues: the pinned seed is exposing a real ~25% failure rate in `updateTraffic`, not causing it. Changing the seed hides the defect. — 2026-08-07, Claude Code

## Known issues
- **`vehicle-sanity.test.js`'s "seeded civilian traffic … without embeds or pileups" case is not seed-robust.** *(Note: this one PASSED in the full-suite run — it fails when the file is run alone. Its instability is context-dependent as well as seed-dependent.)* It asserts `maxClosePairs < 4 && massPileupFrames === 0` against one pinned seed (`424242`). PV3 changed how many RNG draws city build consumes (tree placement now rejection-samples), which re-rolls the world for every seed — seed `424242` now lands on a layout where civilian traffic transiently clusters (6 cars within 2.2u for 51 of 1200 frames; `carsWithEmbeds: 0`, so nothing is actually stuck).
  **This is not a regression PV3 introduced in the traffic AI.** Measured with the real test harness, one seed at a time: `main` **fails on seeds 99, 11, 44 and 77** (4 of 16 seeds tested — 25%), and this branch passes on 5150/777/31337/99 and fails only on 424242. The pileup is a pre-existing, layout-dependent property of `updateTraffic`; the pinned seed simply used to dodge it. Repro either way: `sed -i 's/seed=424242/seed=99/' tests/cases/vehicle-sanity.test.js && cd tests && node run.js vehicle-sanity` on a clean `main`.
  Not fixed here: it's OP2-B's area (Codex), and AGENTS.md §4a says log an unrelated failure rather than fix it unasked. The real fix is either traffic separation/spacing in `updateTraffic`, or making the case sample several seeds instead of trusting one. **Do not "fix" it by editing the pinned seed to one that passes** — that hides a 25% real failure rate. — 2026-08-07, Claude Code
- `FOOTBALL_STRAND.md`'s internal section cross-references are wrong. §4's mission specs cite "(§5)" for the `jock_street_taunt` / `jock_fight` / `coach_taunt` / `coach_defeat` dialogue hooks; those packs are actually in **§7** (line 425+), and §5 is the Turbo Bowl design. The numbering drifted when the doc was expanded and the pointers were never resynced. Follow the pack *names*. Recorded, not fixed — renumbering is a doc-wide edit and nobody asked for one. — 2026-08-02, Claude Code
- ~~PII in the docs~~ **RESOLVED — owner's decision, 2026-07-24:** the owner's first name in `HANDOFF.md`/`GAME_PLAN.md`/`ASSETS.md`/`STORY_BIBLE.md`/`CHARACTERS.md` is fine and does not need scrubbing. Do not re-flag it. The rest of AGENTS.md §3 still applies in full — no surnames, emails, phone numbers, addresses, other-platform usernames, or credentials of any kind. Latest scan found none of those. — 2026-07-24, Claude
- Duplicate `## 10.` heading in HANDOFF.md ("Suggested Order of Work" and "Changelog — polish pass (Kimi3)"). Still open: renumbering the duplicate cascades through §§11–15 and every cross-reference to them, so it is not the one-line fix it looks like. Cosmetic only — the NEXT-marker workflow is unaffected. Leave it unless the owner asks. — 2026-07-24, Claude
- Park knolls keep ~0.15u of sag between the drawn lawn and `groundH` (a dome sampled every ~3u). Harmless at that size; only worth revisiting if props on a knoll ever look sunk. — 2026-07-24, Claude
- `tests/cases/save-restore.test.js` (and later files in a long run) time out loading the page in the local Playwright Chromium headless shell, with GPU stall warnings in the browser log. The same suite runs fine in other contexts; appears environmental. Syntax check, economy suite, regression, and mission-variety all pass. — 2026-07-25, OpenCode Same shape on 2026-07-28: `shadow-footsteps.test.js`'s audio-synthesis case failed once at the tail of a full 198-case run and passes 5/5 on its own. — 2026-07-28, Claude Code Recurred identically on 2026-08-02 (that same case, tail of a 207-case run, 5/5 alone) — it is reproducible enough now to call it the suite's one standing flake, not a one-off. — 2026-08-02, Claude Code Recurred again 2026-08-07 at the tail of a 278-case run, same case, 5/5 alone. Three sessions, same signature: it is load-dependent, not a real defect. — 2026-08-07, Claude Code
- The suite baseline is **288 cases across 57 files** as of 2026-08-11 (counted, not quoted: `node -e` over `tests/cases/*.test.js`). Older notes quoting 36/43/53/70/77/197/198/207 are all historical — ignore them, and re-count rather than trusting any number written down here. — 2026-08-11, Claude Code

## Archive
- Owner playtest bug batch BUG1–BUG6 (Claude Code | Opus 5 | high, 2026-08-11): six reported symptoms, four causes. **Camera stutter** was a cut, not a stutter — `cameraCollide` reported occluders only in tenths of the boom and `updateCamera` applied the whole correction in one frame (measured 5.98u in a single frame when a building corner swept the boom); now bisected and speed-capped at 22 u/s. **Punch/kick "not reacting"** — the strike fired on the button *release*, so a tap sat dead until you lifted your thumb and a press-and-hold gave nothing for a full second; it now lands on the press, holding still escalates to the special, and a press supersedes a stale hold (one lost pointerup used to wedge melee off for the whole session). **Heli pitch, car roll and Turbo's hill lean were one defect**: three meshes composed attitude on THREE's default `XYZ`, which applies `rotation.x` about the world axis *after* the heading — so pitch only meant pitch on a north/south heading and became body roll on an east/west one. All three now `YXZ`; no attitude values changed. **Fall deaths** left a puff of grey smoke — added a pooled gore block to §CARNAGE (chunks off the existing bounded debris pool, a red spray, a blood pool that spreads then soaks away). `tests/cases/playtest-bugs.test.js` added, 10/10. Signed: Claude Code \| Opus 5 \| high
- PV production-value pass (Claude Code | Opus 5 | high, 2026-08-07): PV1–PV11 — §POST FX, §SLOW MOTION, §ACTION BAIL-OUT, §CARNAGE, §RAMPAGE, a `groundH` shore term, Coast Highway paint, skyline heights and bike riders; 11 new test files. PV5 shipped half (skyline yes, footprint no). Signed: Claude Code \| Opus 5 \| high
- PV follow-up (Claude Code | Opus 5 | high, 2026-08-10): fixed the two CI failures the PV branch was carrying, both real regressions rather than the flaky tests a previous session had logged them as. *Stair descent* — a fire escape's walkable run started at `b.baseY`, but the flight stands 1.3u outside the wall where graded ground sits higher, so the bottom steps were buried and `baseH` sat 0.85u below the terrain you land on; runs now start at `max(b.baseY, groundH(stair foot))`. *Gait* — `updateFoot` treated any gap over 0.001u as a fall, so PV4's shore slope produced 13 spurious `footLand()` thumps in 11 frames, flipping the foot parity; a travelled-scaled step-down (capped 0.6u) is now absorbed instead. Also repaired `bail-out.test.js`, which sampled `player.dive.phase` *after* the call that can both roll and end in the same frame, and pinned no seed. Signed: Claude Code \| Opus 5 \| high
- Full-game script (Claude Code | Opus 5 | high, 2026-08-07): `SCRIPT.md`, `SCRIPT_STRANDS.md`, `SCRIPT_CAST.md`, `SCRIPT_BARKS.md`, `SCRIPT_TURBO_TTS.md`, `KIMI_TURBO_VO.md` — Ch1 three-way branch, Ch2–5 spine + 3 endings, 6 strands, 44 cutscenes, 413 lines (239 flat-numbered for Turbo TTS) plus the Kimi voice-acting handoff generated from the line file so it can't drift. Docs only. One canon override on owner instruction: the kid is unnamed (retires `CHAPTER1.md`'s Mookie). Signed: Claude Code \| Opus 5 \| high
- FB4 Turbo Bowl + FB5 payoff (Claude Code | Opus 5 | high, 2026-08-02): endless-run minigame gated on `G.coachBeaten` — PLAY BALL beacon, 100-yard run, converging Alumni Wildcats, soft-fail tackles, persisted best. First touchdown plays `turbo_bowl_payoff`, built to HANDOFF's FB5 card (no Dad actor) on the owner's ruling over `FOOTBALL_STRAND.md` §6. `turbo-bowl.test.js` 8/8. Signed: Claude Code \| Opus 5 \| high
- FB3 Coach mission (Claude Code | Opus 5 | high, 2026-08-02): Old Scores → Rematch; Coach yields rather than dies, a loss soft-retries at the field, the win sets and saves `G.coachBeaten` and the Wildcats go non-hostile. `fb3-coach.test.js` 10/10. Signed: Claude Code \| Opus 5 \| high
- Session-cost + concurrency tooling (Claude Code | Opus 5 | high, 2026-08-02): `tools/codemap.js` (generated code map, CI-gated), `tools/preflight.js` (cross-account claim visibility), AGENTS.md §0 facts table / §2a two-account protocol / §4a testing tiers, and corrected `index.html` size figures that were ~2.4× wrong. Signed: Claude Code \| Opus 5 \| high
- Test reconciliation (Claude Code | Opus 5 | high, 2026-07-28): retired the mushroom-cloud case in `terra-followup.test.js` and moved `new-features.test.js` off its 30s fuse literal onto `CAR_CRITICAL_FUSE` — both asserted behavior the owner's OP2-G direction deliberately removed. Signed: Claude Code | Opus 5 | high
- Backlog bookkeeping (Codex | GPT-5 | high, 2026-07-25/26): the four-agent OP2 dispatch packet, the owner's Mama Rat / playtest-correction / shadow-footstep / Turbo Mode requirements, and the optional audit follow-up, all recorded as owner-triggered future work. Signed: Codex | GPT-5 | high
- Consolidation (Claude Code | Opus 5 | high, 2026-07-28): merged all five OP2 branches in packet order, resolved the one real code overlap (fall-impact vs. footstep landing in `updateFoot`) and three bookkeeping conflicts, regenerated the `index.html` code map, and marked Phase 11 OP2-A–G done. Signed: Claude Code | Opus 5 | high
- OP2-F cinematic routes (Claude Code | Opus 5 | high, 2026-07-28): shots planned whole — clear waypoints, centripetal spline, eased arc-length travel, framing smoothed separately. Intro peak accel 75,018→8 u/s², look snap 1892→1.4 rad/s²; staged placements 28/36 unsafe→0/336 over 4 seeds. `tests/cases/cinema-routes.test.js` added. Signed: Claude Code | Opus 5 | high
- OP2-E mission UI + bubble tails (Claude Code | Sonnet 5 | medium, 2026-07-28): mission offers/status became translucent top-center pills clear of the reticule, STEAL CAR untouched at 158×52; one shared `orientBubbleTail` points every tail at the speaker's projected head. Signed: Claude Code | Sonnet 5 | medium
- OP2-C/OP2-D melee + grounding (Claude Code | Opus 5 | high, 2026-07-28): ordinary punch/kick were gated on the charge test and died while crouched/indoors/airborne — gates split, `endMelee()` at every exit from the foot sim; charged kick pivots at the hip into the planted horizontal pose; shadows stay flat on `groundH` and smear out on knockdown; gait-phase footsteps over 4 surfaces + landing thump. `tests/cases/shadow-footsteps.test.js` added. Signed: Claude Code | Opus 5 | high
- OP2-G car failure + lethal falls (Claude Code | Sonnet 5 | high, 2026-07-28): critical fuse 30s→15s (`CAR_CRITICAL_FUSE`), mushroom cloud gone, one slowed blast at `CAR_BOOM_RADIUS`=10; a real ≥4×`turboHeight()` drop onto solid ground routes into the existing `wasted()`. `tests/cases/fall-damage.test.js` added. Signed: Claude Code | Sonnet 5 | high
- OP2-A roads + sidewalks (Claude Code | Sonnet 5 | high, 2026-07-28): baked canvas dashes and manhole dot replaced with world-scale geometry (3u dash / 9u gap, torus rim + recessed lid, `MANHOLE_SPOTS` untouched); the 49u curb strip is 8 `groundH`-sampled pieces per edge, so sidewalks follow grade. `tests/cases/road-sidewalk.test.js` added. Signed: Claude Code | Sonnet 5 | high
- OP2 five-agent dispatch (Claude Code | Opus 5 | high, 2026-07-27): `DISPATCH/OP2_FINISH/` packet — pinned models/efforts/branches, code ownership, self-claim protocol and integration order for the six remaining Phase 11 cards. Signed: Claude Code | Opus 5 | high
- OP2-B vehicle sanity (Codex | GPT-5.6 | high, 2026-07-26): seeded traffic 185.6→45.4 full-lock frames/car, 19,402→0 embed frames, 30/30 jackable; speed-monotonic impact damage once per contact. Merged to `main` as #41. Signed: Codex | GPT-5.6 | high
- Consolidation (Codex | GPT-5 | high, 2026-07-25): reconciled all five OP1 commits, corrected the amended mission/HUD hash, regenerated the code map, advanced NEXT to FB3, and passed the final 800×390 OP1 touch smoke 1/1. Signed: Codex | GPT-5 | high
- OP1 completion (Codex | GPT-5 | high, 2026-07-25): dog mortality/30s capped ghosts, shared elevated intro camera and terrain-safe Deb shots, mission opt-in with safe HUD/wanted stars; focused checks passed. Signed: Codex | GPT-5 | high
- OP1 controls and charged melee (Codex | GPT-5 | high, 2026-07-25): fresh sprint input survives vehicle exits; aim and melee cameras ease smoothly; one-second punch/kick holds launch bounded three-round specials. Syntax, sprint 3/3, charged melee 2/2, and camera polish 7/7 green. Signed: Codex | GPT-5 | high
- OP1 planning (Codex | GPT-5 | high, 2026-07-25): made the owner playtest batch authoritative before FB3 and specified five sequential implementation commits; dog, cinematic-camera, and mission/HUD commits remain open and unclaimed. Signed: Codex | GPT-5 | high
- Consolidation (Codex | GPT-5 | high, 2026-07-25): replayed the six post-PR #40 planning/OD commits onto current `main`, resolved bookkeeping-only conflicts, and archived the open branch records. Signed: Codex | GPT-5 | high
- OD1–OD4 gameplay pass (Codex | GPT-5 | high, 2026-07-25): straight swept RPGs, building-occluded world bubbles, hold sprint with a distinct pose, and bounded local street-density refills; focused checks green. Signed: Codex | GPT-5 | high
- Backlog planning (Codex | GPT-5 | high, 2026-07-25): specified and delivered OD1–OD4, deferred A2 by owner direction, and restored FB3 as the next canonical task. Signed: Codex | GPT-5 | high
- R2/R3 closeout (Codex | GPT-5 | high, 2026-07-25): archived pooling, spawn-safety, recovery, handoff, and focused CI-stabilization records already delivered through PR #40. Signed: Codex | GPT-5 | high
- Consolidation (Codex | GPT-5 | high, 2026-07-25): archived the completed PR #40 batch, reconciled U3 as DONE, and advanced the authoritative NEXT marker to R2. Signed: Codex | GPT-5 | high
- U3 recovery flow (Codex | GPT-5 | high, 2026-07-25): BUSTED/WASTED penalties persist before the respawn lock; pursuit clears and focused coverage verifies playable terrain-seated recovery. Signed: Codex | GPT-5 | high
- Audio/RNG/CI (Codex | GPT-5 | high, 2026-07-25): lazy cached VO, shared seeded RNG, broader syntax checks, and GitHub Actions. Signed: Codex | GPT-5 | high
- PR #40 gameplay pass (Codex | GPT-5 | high, 2026-07-25): camera/terrain/controls/dog lifecycle, solid mushroom cloud, MP3-only Turbo dialogue, and focused coverage. Signed: Codex | GPT-5 | high
- Terra follow-up (Codex | GPT-5 | high, 2026-07-25): solid mushroom cloud, MP3-only Turbo paths, regenerated code map, and focused tests. Signed: Codex | GPT-5 | high
- Terra handoff refresh (Codex | GPT-5 | medium, 2026-07-25): captured exact PR #40 validation and the next-step guidance. Signed: Codex | GPT-5 | medium
- City Glow (Kimi K3, medium, 2026-07-25): lit-window night swap, instanced neon signs on street-facing facades, grade-tilted streetlight glow pools; `facadeMats` buckets wired into `applyDayNight`. `index.html` §CITY GLOW; `tests/cases/city-glow.test.js` added. Merged to `main` as #37; suite 3/3 + boot green.
- P2 Economy tuning (OpenCode | Kimi K2 | high, 2026-07-25): flattened heist loot ($250–$500 + $150 escape), raised stickup/store/delivery/pizza payouts, routed raw payouts through `addMoney()` for persistence and gold-class feedback. `index.html` §HUD/TOASTS, §WEAPONS, §STORY, §DAY/NIGHT, §MISSIONS; `tests/cases/economy.test.js` added. Merged to `main` as #38; economy suite 6/6 + regression + mission-variety green.
- Intro camera terrain clamp (Kimi K3, medium, 2026-07-25): `flySample()` clamps flythrough cam + look target above `groundH`; Cinema Mode free-fly floor uses `groundH+0.5`. `index.html` §ANIMATED INTRO / §CINEMA MODE; `tests/cases/intro-camera.test.js` added. Merged to `main` as #36; suite 3/3 + cinema 6/6 + boot green.
- Controls card onboarding (Kimi K3, medium, 2026-07-25): first-boot TOUCH/DESKTOP tabs, `controlsCardSeen` in save blob, pause HOW TO PLAY recall (replaced `pmHow`). `index.html` §CONTROLS CARD; `tests/cases/controls-card.test.js` added. Merged to `main` as #35; suite 5/5 + save-restore + boot green.
- Cinema Mode (Opus 5, high, 2026-07-24): the REPLAY screen grown into a director interface — live free-fly camera over the running world, scene dropdown (intro flythrough, 4 scripted convos, jock fight, blow up a car/cop car, shoot a ped, rat mother), HIDE HUD for clean recording, director invulnerable while filming, old 30s buffer scrub kept as one scene option. `index.html` §CINEMA MODE (was REPLAY) + §CINEMA: SCENES & STAGING; `tests/cases/cinema-mode.test.js` added. Merged to `main` as #29; suite 105/105 green on the merge.
- Radio towers (Opus 5, medium, 2026-07-24): `index.html` §RADIO TOWERS — one 132-unit guyed mast on an open parking block plus up to 7 rooftop masts on buildings ≥30 tall, red/white banded with blinking obstruction lamps (steady mid-mast markers on the big one). Fully instanced, decorative only — not added to `buildings`, so collision and stairs are untouched; `updateRadioTowers()` is called next to `updateLights()` in the main loop, outside the cinema gate. Merged to `main` as #33.
- Player-facing polish (Sonnet 5, medium, 2026-07-24): U1 story-objective HUD (find Deb / pay the debt, live distance) + Deb minimap marker and blip legend; P1 three new mission types (courier/takedown/getaway) gated by a `missionsDone` tier; J4 BRAKE/REVERSE relabel; J3 look-sensitivity slider + invert-Y and a faster low-speed car camera; P3 Easy/Normal/Hard scaling cop pressure, detection range and damage taken. See HANDOFF.md §§16–19.
- Terrain (Claude, high, 2026-07-24): hills + graded streets, landed over three passes. Final state — `groundH` is a continuous lattice: streets run level across their width and grade only along travel, each block is the bilinear patch joining its four kerb lines, signature hills sit inside a 12° grade budget. Ground and beach meshes are built on the road lattice so the drawn surface matches the field; every static reads its Y from `groundH`. Terraced pads, retaining walls and seam stairs were tried and removed — see the revision note in `TERRAIN.md`. Merged to `main` as #31; suite 99/99 green on the merge.
- Audio — weapons (Sonnet 5, medium, 2026-07-24): `WEAPON_SFX` registry — pistol crack, RPG launch/flight/boom, reload, melee (fists/baton) voices, dedicated `sfx.carBoom`.
- Audio — creatures (Sonnet 5, medium, 2026-07-24): Mama Rat screech/bite/death and stray-dog growl/bite cues, replacing reused `sfx.punch()`/`sfx.bigCrash()`.
- Audio — vehicles (Sonnet 5, high, 2026-07-24): multi-layer engine synth (sub/grit/turbo per car type, scaling with damage and boost) and a noise-fed helicopter rotor chop.
- Animation (Opus 4.8, medium, 2026-07-24): Turbo's stair-climb stride (airborne test now measured against the floor under his feet, not y=0), plus wall-ladder climbing up 12 buildings with a validated climb corridor.
- City beautification (Sonnet 5, high, 2026-07-24): 3D kerb strips, planters, awnings, street trees, café tables, pole banners — density-graded, cosmetic, seated via `groundH`.
- NPC types (Haiku 4.5, low, 2026-07-24): 303 character archetypes across 8 batches for diverse NPC spawning.
- Docs & tooling (Haiku 4.5, 2026-07-24): backlog reconciled against real code (W1), `NEXT: P2` marker (W5), fast pre-flight test tier — `tests/syntax-check.js` + `smoke.test.js` (W3), plus specs written but not implemented: economy audit (P2), onboarding controls card (U2), accessibility options (A2), brake-vs-reverse clarity (J4).
- Navigation (Claude, high, 2026-07-24): `index.html`'s `// CODE MAP` recomputed from the real banners (was ~58 lines adrift, missing 3 sections) and HANDOFF.md §5 re-synced to every banner in file order.
- Bug-fix pass (OpenCode | Kimi K2 | high, 2026-07-25): terrain-aware jump via `footGround()`, sidewalk trees, killable jocks/cops/foot-cops; `tests/cases/regression.test.js` added/extended.
- D5 Time controls (OpenCode | Kimi K2 | medium, 2026-07-25): dev-only `TIME_SCALE`/`STEP_FRAMES`, 1×/0.25×/4× hotkeys + dev-panel buttons, debug-HUD speed readout; `tests/cases/time-controls.test.js` added.
- D7 Deterministic seed (OpenCode | Kimi K2 | medium, 2026-07-25): central `_rng` with optional `?seed=<n>` mulberry32 PRNG; all `index.html` randomness routed through it; `tests/cases/deterministic-seed.test.js` added.
- J2 Hitstop + refined shake (OpenCode | Kimi K2 | medium, 2026-07-25): `HIT_STOP` freeze on big impacts, magnitude-squared camera shake with speed-sensitive decay, `SETTINGS.reduceMotion` toggle in pause settings; `tests/cases/hitstop.test.js` added.

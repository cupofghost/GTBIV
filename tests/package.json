{
  "name": "gtb4-tests",
  "private": true,
  "description": "Headless regression suite for GTB IV. Lives outside the game's zero-build deploy — see tests/README.md.",
  "scripts": {
    "syntax": "node syntax-check.js",
    "preflight": "node preflight.js",
    "test": "node run.js"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
